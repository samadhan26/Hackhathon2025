name: hackathon
description: A simple counter app using GetX

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6  # Add GetX package
  cached_network_image: ^3.3.1
  flutter_spinkit: ^5.2.0
  google_fonts:
  lucide_icons:
  dropdown_button2:
  speech_to_text: ^6.3.0
  flutter_tts: ^3.8.5
  flutter_dotenv:
  fluttertoast: ^8.2.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  intl: ^0.18.1   # or latest version



flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - .env
